/*
 * Handle keypresses
 */

//document.addEventListener("backbutton", onBackKeyDown, false);

const keypress = () => {
  e.preventDefault();

  if (state === 1) {
    //game.backButton();
  } else if (state === 2) {
    //game.backButton();
  } else if (state === 3) {
    //
  }
};
